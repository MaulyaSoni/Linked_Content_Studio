# ─────────────────────────────────────────────────────────────────────────────
# .env.template — LinkedIn Post Generator + Agentic AI Content Studio
# ─────────────────────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your credentials.
#   cp .env.template .env
#
# Never commit .env to version control!
# ─────────────────────────────────────────────────────────────────────────────

# ── Required: Groq LLM API ──────────────────────────────────────────────────
# Get your key at: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# ── Optional: LinkedIn API (for posting directly from the app) ───────────────
# Instructions:
#   1. Go to https://www.linkedin.com/developers/apps and create an app.
#   2. Add "Share on LinkedIn" and "Sign In with LinkedIn" products.
#   3. Generate an access token via the OAuth 2.0 Authorization Code flow.
#   4. Your User ID is the "sub" field in the /v2/userinfo endpoint response.
#
# Without these values, the app still works — posting will show an error,
# but content generation runs normally.

LINKEDIN_ACCESS_TOKEN=your_linkedin_access_token_here
LINKEDIN_USER_ID=your_linkedin_user_id_here
LINKEDIN_CLIENT_ID=your_linkedin_client_id_here
LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret_here

# ── Optional: GitHub (for private repo loading) ──────────────────────────────
# Get a Personal Access Token at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here
